# docs/mf_dev — 설계·구현 문서 (MF_PiFinder)

포크가 개발하며 남긴 **설계·구현 설명, 계획, 설치·운영 가이드, 프로세스**
문서를 모아둔다. 현장 테스트·실측 리포트는 [`../mf_report/`](../mf_report/)에 있다.

주제별 목록과 문서별 상태(living / install / plan)는 인덱스를 본다:
[한국어](../mf_docs_index_ko.md) · [English](../mf_docs_index_en.md).

작업 전 해당 영역 문서를 먼저 읽는다 — 코드만 보고 의도를 역추론하지 않는다.
