# 2026-09-08 토성 추적 실측: 저고도 지연과 GoTo 관측 시각 보호

상태: **1차 현장 수집·보호 수정 적용 완료. 솔빙 속도 최적화와 독립 정밀도 검증은 후속 과제**.
기준: [통합 작업서](../mf_dev/mf_solver_goto_observation_workplan_20260908_ko.md).
실측 시각: 2026-09-08 21:52~22:13 KST. 기준 HEAD `d9edcd72` + 기존 로컬 변경.

## 목표와 현장 조건

사용자가 토성을 추적하는 중 저고도·강한 광해에서 시험을 요청했다. 사용자는 좌우 움직임을 보고했고,
자동 보정을 잠시 끈 뒤 왕복이 멎었다고 답하면서 **흔들림의 원인은 바람**이라고 설명했다.
따라서 자동 보정 OFF 전후의 관측만으로 물리적 흔들림을 소프트웨어 탓으로 결론 내리지 않는다.
고고도 비교는 사용자가 직접 이동 완료를 확인한 뒤 시작했다.

전처리 실행 간격, 왜곡 k1(−0.11), 품질·연속성 기준, 노출 제어 설정, 최종 허용 오차(3분각)를 변경하지 않았다.
자동 보정 `indi_tracking_guide_enabled`만 시험 중 일시적으로 끄고 수정 적용 후 원래 true로 복구했다.
처음 수집 당시와 복구 후 config.json의 값 차이는 없었다. 자동 노출의 실제 노출 시간은 구간마다 달랐다.

## 1. 기록 결과

| 구간 | 승인/시도 | 입력 확보→게시 중앙값 / p95 | 유효 게시 최대 공백 | RAW |
| --- | --- | --- | --- | --- |
| 저고도 첫 구간, 보정 OFF 전환 포함 | 1/102 | 2.342 / 2.342초 | 승인 1개라 계산 불가 | 96장, 대응 RAW 없음 6회 |
| 저고도 보정 OFF | 12/23 | 3.610 / 4.067초 | 7.624초 | 18장, 대응 RAW 없음 5회 |
| 고고도 기준, 보정 OFF | 112/120 | 0.173 / 0.338초 | 1.552초 | 120장 |
| 보호 수정 후 고고도, 보정 OFF | 95/98 | 0.383 / 0.596초 | 1.513초 | 96장, 대응 RAW 없음 2회 |
| 수정 후 고고도→토성→추적 전체 | 106/215 | 1.684 / 3.084초 | 33.321초, 큰 이동 포함 | telemetry 모드 |

5개 세션의 solver/integrator 기록은 모두 complete, 무결성 오류 0, 저장 큐 누락 0이었다.
대응 RAW 미확보와 저장 큐 누락은 다르다. 승인과 게시 연결의 세션 경계 누락은 게시 실패로 단정하지 않는다.
고고도 기준은 120시도 제한으로 약 40.7초에 종료됐으므로 ‘60초 전체’ 자료가 아니다.
마지막 전체 세션은 고고도·큰 이동·미세 보정·도착 후 추적을 포함하므로 전체 승인율을 정지 솔빙 안정성으로 사용하지 않는다.

저고도 보정 OFF 구간은 전부 sync였고 고도 중앙값은 약 21.7°였다.
고고도 기준은 약 53.1°이며 120회 중 async 114회, sync 6회였다.
실제 노출은 저고도 OFF 218607µs, 고고도 기준 318518µs, 수정 후 고고도 336577µs였다.
서로 다른 별 영역·노출·시간·바람 조건과 서비스 재시작이 있어 통제된 광해 단독 A/B가 아니다.
고고도 전후의 지연은 오히려 증가했으므로 **이번 수정으로 솔빙 계산 속도가 개선됐다는 주장은 하지 않는다**.

## 2. 별도로 확인한 제어 문제

기존 로그에서 21:51:25, 21:51:50, 21:52:13, 21:52:37, 21:53:00 등에
`No solve anchor within 12s before recovery goto; using pifinder_imu_estimate/medium coordinate`가 기록됐다.
이후 같은 좌표로 Mount SYNC와 복귀 GoTo가 실행됐다. 21:52:37 SYNC의 상태 기록에는
`pointing_source=pifinder_imu_estimate`, `solve_age_seconds=22.9`가 남았다.

이는 ‘12초를 기다렸으므로 새 솔빙이 아닌 추정 좌표도 재정렬 기준으로 허용’하는 실제 실행 경로다.
바람에 의한 흔들림의 원인과 별개로, 재정렬 좌표의 신뢰 근거가 부족해지는 문제로 판단했다.
이 자료만으로 각 복귀 이동이 실제 중심 오차를 얼마나 만들었는지는 확정하지 않는다.

## 3. 구현·적용한 변경

변경 소스: `python/PiFinder/indi_goto_guide_service.py`.

- 자동 복귀 GoTo는 source=solve/quality=high이며 유효한 촬영 timestamp가 있는 관측을 요구한다.
- 관측 시각이 미래이거나 12초보다 오래되면 복귀 SYNC/GoTo를 보내지 않는다. 상태 파일 갱신 시각으로 대체하지 않는다.
- 복귀 솔빙 대기가 12초를 넘겨도 IMU 추정값으로 진행하지 않는다. 자동 보정을 보류하고 새 정상 솔빙이 오면 다시 판단한다.
- 일반 GoTo의 정지 후 솔빙 대기는 기존 12초를 유지한다. 시간 초과 시 추정 좌표로 다음 재정렬·완료 판단을 진행하지 않고 `No fresh plate solve after GoTo`로 종료한다.
- 최근의 정상 정지 후 솔빙이 있으면 기존처럼 즉시 다음 단계 또는 완료로 진행한다.

22:04:23 KST에 PiFinder 서비스를 재시작해 적용했다. 재시작 직전 이동 없음과 자동 보정 OFF를 확인했다.
재시작 후 마운트 연결·기본 추적·솔빙 복구를 확인하고, 22:07:02에 자동 보정을 true로 복구했다.
마운트 컨트롤러/INDI 서버 자체의 재부팅은 수행하지 않았다.

검증:

- GoTo/Guide·mount-control·pointing 관련 테스트 **155개 통과**.
- 오래된/미래/없는 timestamp, IMU 추정 상태에서 대기 시간이 지나도 SYNC/GoTo가 나가지 않는 검사.
- 나중에 정상 솔빙이 도착하면 복귀하는 검사와 정상 정지 후 솔빙의 즉시 완료 검사.
- Ruff 검사·포맷 확인, 대상 모듈 MyPy, diff 공백 검사 통과.
- 실제 고고도 솔빙 복구 및 사용자 요청 토성 GoTo 완료 확인.

이 변경은 작업서 C2/C4의 일부다. **펄스 자체의 노출 구간 검사, 평균 창의 모든 표본 나이 검사,
최초 GoTo 시작 앵커의 전체 정책을 함께 고친 것은 아니다.** 제어 전반의 모든 오래된 좌표 사용을 해결했다고 표현하지 않는다.
솔빙이 없는 대상에서도 12초 뒤 추정 좌표로 계속 재정렬하던 기존 동작은 더 이상 허용하지 않는다.

## 4. 수정 후 토성 GoTo 결과

| 사건 | KST | 기록 |
| --- | --- | --- |
| 첫 SYNC + GoTo | 22:07:34.642 | 정상 solve 기준, 목표까지 약 30.47° |
| 두 번째 SYNC + GoTo | 22:08:09.811 | 정상 solve 기준, 오차 약 40.06분각 |
| 미세 펄스 보정 시작 | 22:08:27.886 | 오차 6.86분각 |
| 최종 완료 | 22:09:41.260 | 표시 오차 2.30분각, 허용 3분각 이내 |

전체 약 **126.6초**, 미세 보정 약 **73.4초**였다. 사용자가 SkySafari 토성 GoTo 도착을 확인했다.
이 GoTo 기록에서는 두 중간 SYNC가 solve 기반이었고 추정 앵커 timeout fallback은 나타나지 않았다.
따라서 정상 경로 회귀 검증은 됐지만, 이번 GoTo 한 번에서 신규 timeout 차단 분기까지 실측으로 발동시킨 것은 아니다.
차단 분기의 동작은 위 모의 테스트로 검증했다.

관측 시각으로 구간을 나누면:

| 구간 | 승인/시도 | 게시 간격 중앙값 / p95 / 최대 |
| --- | --- | --- |
| 미세 보정 | 24/29 | 2.515 / 5.528 / 9.025초 |
| 도착 후 추적 | 29/30 | 2.511 / 4.022 / 5.514초 |

큰 이동 중 미승인 98개는 moving=true였다. 이를 정지 솔빙 실패와 합쳐 해석하지 않는다.
보정 중 읽힌 펄스에는 최대 길이 2500ms의 north/west 명령이 반복됐다.
0.2초 상태 polling은 덮어쓴 짧은 이벤트를 놓칠 수 있으므로 펄스 총수나 실제 종료 시각의 완전한 기록은 아니다.
표시 오차는 중간에 3.1→5.0분각으로 다시 증가한 뒤 내려왔다. 이 상승의 원인을 바람·좌표 변동·실제 이동 중 하나로 확정하지 않는다.
마지막 상태 확인에서 자동 보정 enabled, 토성 추적 상태와 표시 오차 약 0.55분각을 확인했다.

위 오차는 PiFinder 내부 좌표 기준이다. 독립 카메라/접안부 스케일에 따른 절대 중심 오차 측정은 없으므로
0.55분각의 절대 지향 정밀도가 입증됐다는 의미는 아니다.

## 5. 다음 속도 개선 작업의 순서

1. **미세 보정 73초 구간의 연결 계측을 완성한다.** 각 펄스가 실제로 사용한 frame ID, 노출 시작/종료,
   전송·예상/확인 종료 시각, 단일 해와 평균 해를 직접 기록한다. 현재 polling만으로는 늦은 영상 재사용을 확정할 수 없다.
2. **저고도에서 반복 실패하는 중심 경로의 비용을 줄일 방법을 비교한다.** 저고도 OFF 입력에서
   preprocessed Cedar center는 18회 모두 실패했고 시도 시간 중앙값은 301ms,
   SEP center도 18회 모두 실패하며 중앙값 430ms였다. 최종 SEP full은 15회 중 11회 품질 해를 만들었다.
   전체 시야의 잔차·연속성·정렬 지점 정밀도를 유지하는 조건으로 실패 이력 기반 재탐색 예산/순서를 검토한다.
   이 수치는 각 경로 조건부 중앙값이므로 단순 합을 예상 절감 시간으로 단정하지 않는다.
3. **전처리 복구의 전경 대기와 누적 재워밍업을 줄인다.** 새 RAW 처리와 병행할 때 프레임·관측 시각·세대를 보존하고
   CPU 경합을 측정한다. 전처리 실행 간격 변경은 계속 제외한다.
4. **큰 보정과 펄스 보정의 전환 시점을 평가한다.** 이번 6.86분각 진입에서 73초가 걸린 이유를 바탕으로,
   유효 좌표 공급 간격과 펄스의 실제 이동량을 고려할 수 있다. 한 번의 결과로 near threshold나 guide 강도를 새 기본값으로 정하지 않는다.
5. 현재 RAW 미확보 시 bias 적용 일관성 문제와 평균/단일 좌표 판단 차이도 남아 있다.
   연속성 검사를 약화하거나 이동 평균을 무조건 늘리는 방식은 사용하지 않는다.

이번 판정: 보호 수정은 적용 유지, 정상 GoTo 완료 확인. **속도 개선율은 아직 미검증**이며 동일 조건 반복 시험이 필요하다.
사용자 확인과 기록을 바탕으로 현재 토성 관측을 계속할 수 있도록 자동 보정 설정은 원래 상태로 복구했다.
이번 관측 수정은 로컬 소스와 실행 프로세스에 반영했으며 서버 푸시는 수행하지 않았다.

## 6. 자료 위치와 재현

분석 폴더: `/home/pifinder/PiFinder_data/captures/analysis/20260908_saturn/`.

| 폴더 | session ID |
| --- | --- |
| A_low_01 | `81e78274d11041598cf49f4418ed7ced` |
| B_guide_off_01 | `2cf1c61ecc9547b28a5dc93594403e79` |
| A_high_01 | `250e7974b0674b5bb80340d97434033a` |
| B_high_guard_01 | `11414a7d79ee4ec38006296910451ae7` |
| C_saturn_guard_01 | `56fabd25304945d38555b3d3e3610aea` |

각 폴더에는 config, 소스 사본·해시, 시작/종료 상태, runtime.jsonl, 전/중 로그, 무결성 보고서가 있다.
`summary.json`, `goto_phase_summary.json`, `goto_observed_timeline.json`, `operator_notes.json`,
`guide_pause.json`, `guide_restore.json`, `saturn_arrival.json`, `final_runtime.json`을 함께 보존한다.
`collect.py`는 제한 시간 수집 스크립트이고 `summarize.py`는 기존 세션만 읽어 집계한다.

`A_high_01` 시작 때 guide 소스는 수정 중이었으나 실행 프로세스는 구버전이었다. 해당 note에 이를 명시했으며,
실제 적용 경계는 `deploy_anchor_guard/deployment.json`과 서비스 시작 시각이다.
변경 전 guide 소스는 `A_low_01/source/indi_goto_guide_service.py`에 보존했다.
도착 확인 답변 시각은 실제 완료보다 늦었고 recorder 제한에 도달한 뒤여서, 구간 분리는 답변 시각 대신 장치 로그 완료 시각을 사용한다.
